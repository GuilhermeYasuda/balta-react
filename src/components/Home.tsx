import React from "react";
import TodoList from "./TodoList";

const Home = () => {
    return (
        <div>
            <h4>Minha lista de tarefas</h4>
            <TodoList></TodoList>
        </div>
    );
}

export default Home;